server:
  port: 9000
spring:
  application:
    name: producer
#  profiles:
#    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"